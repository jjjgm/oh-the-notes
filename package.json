{
  "name": "note-taker",
  "version": "1.0.0",
  "description": "This creates an application named \"Note Taker\" that can be used to write and save notes- using Express.js back end and saves and retrieves the note data from a JSON file.",
  "main": "server.js",
  "scripts": {
    "start": "nodemon src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "keywords": []
}
